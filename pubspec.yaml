name: bmi088_app
description: BMI088 BLE sensor monitor
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  permission_handler: ^11.3.1
  device_info_plus: ^10.1.0   

flutter:
  uses-material-design: true